# NP-018 — IPC request routing
- **Stage:** S1 — Core IPC
- **Area:** request-router
- **Dependency:** NP-017
- **allowed_paths:** `crates/ipc/, apps/core/, crates/core/, docs/`
- **Primary file-level target:** `crates/ipc/src/lib.rs; apps/core/src/main.rs; crates/core/src/lib.rs`
- **Goal:** IPC request routing
- **API / Interface:** CoreRuntime, IpcEnvelope, NamedPipeServer/Client, request/event contracts

## Test Matrix
- Unit: deterministic happy/error cases for `request-router`.
- Integration: verify the stage API boundary and dependency behavior.
- Negative: malformed input plus timeout/cancellation/unavailable dependency where applicable.
- Regression: preserve existing workspace and versioned contract behavior.
- Windows: required when touching TUN, process attribution, privileges or desktop integration.

## Completion Acceptance Commands
```powershell
cargo fmt --all -- --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

## Codex Prompt
Read README.md, AGENTS.md, docs/ARCHITECTURE.md, docs/IPC_PROTOCOL.md and this task.
Plan first. Implement only NP-018. Modify only allowed_paths. Establish stable types/interfaces
before wiring behavior. Add deterministic tests, explicit error handling, timeout/cancellation for
network work, and secret redaction. Run acceptance commands. Summarize files, tests, risks and
human-review needs. Do not expand into later tasks.

## Definition of Done
Build passes; relevant tests pass; error paths are covered; network operations have
timeout/cancellation; secrets are redacted; contracts remain compatible; docs/ADR are updated
when architecture changes; Windows-sensitive work receives human review.
