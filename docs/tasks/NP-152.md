# NP-152 — Structured logging

- Stage: F0 — Bootstrap & Process Wiring
- Dependency: NP-151
- Primary target: `crates/core/src/logging.rs`

## Goal
Implement **Structured logging** as real NetPilot functionality. Do not treat a page opening or a source file existing as completion.

## Required behavior
- Integrate with the existing V4 + S11 architecture.
- Keep GUI as control plane and Core as runtime authority.
- Preserve versioned IPC/config compatibility unless this task explicitly changes it.
- Add deterministic unit/integration tests.
- Network and Windows operations require bounded timeout, cancellation, cleanup, and error handling.
- Never log passwords, tokens, private keys, proxy credentials, or sensitive subscription URLs.
- Complex proxy protocols must use mature/auditable implementations; do not invent cryptography.

## Completion commands
- `cargo fmt --all -- --check`
- `cargo test --workspace`
- Relevant Windows smoke/E2E test when applicable.

## Codex / Claude Code prompt
Read `README.md`, `AGENTS.md`, `AGENTS_V5.md`, `docs/ARCHITECTURE.md`, `docs/IPC_PROTOCOL.md`, `docs/TASKS.md`, `docs/TASKS_V5.md`, then inspect the existing V4 + S11 implementation.

Plan first. Modify only the necessary files and tests. Implement **Structured logging** in `crates/core/src/logging.rs`. Report files changed, interfaces changed, tests added, exact commands/results, and known limitations. Do not claim completion from compilation alone.
