# NP-264 — NetPilot 1.0 final release acceptance

- Stage: R1 — Release
- Dependency: NP-263
- Primary target: `tests/release/final/`

## Purpose
Execute this release-engineering task against the completed V5 implementation. A source file existing or the GUI launching is **not** proof of completion.

## Acceptance
- The production artifact/configuration is actually built or tested as applicable.
- `cargo fmt --all -- --check` passes where Rust applies.
- `cargo test --workspace` passes where applicable.
- Relevant Windows build/smoke/E2E test passes.
- No passwords, tokens, private keys, proxy credentials, or sensitive subscription URLs are exposed.
- Exact commands, results, artifact paths and known limitations are recorded.

## AI Coding Prompt
Read `README.md`, `AGENTS.md`, `AGENTS_V5.md`, `docs/ARCHITECTURE.md`, `docs/IPC_PROTOCOL.md`, `docs/TASKS_V5.md`, `V5_MANIFEST.json`, and the completed V5 implementation.

Implement **NetPilot 1.0 final release acceptance** in `tests/release/final/`. Do not redesign working architecture. Inspect before editing. Modify only necessary files and tests. Never claim a gate passed without executing it. Preserve user data during install/upgrade/uninstall unless explicitly documented. For TUN/Wintun/UAC/driver work, fail safely and require human review.

Report:
- files changed
- artifacts produced
- commands actually executed
- exact pass/fail results
- security/stability findings
- known limitations
