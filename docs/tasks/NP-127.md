# NP-127 — Subscription Update Policy

- **Stage:** S11 — Airport Subscription Enhancement
- **Dependency:** NP-126
- **allowed_paths:** `crates/subscription/, crates/config/, crates/proxy/, crates/protocols/, crates/transports/, crates/ipc/, apps/desktop/, tests/, docs/` (only paths relevant to this task)
- **Primary file-level target:** `crates/subscription/src/update_policy.rs`
- **Goal:** 手动、启动、定时自动更新策略

## API / Interface
Integrate with the existing V4 unified `ProxyProfile`, `ProxyGroup`, configuration,
IPC and routing contracts. Do not create a competing proxy model.

## Test Matrix
- Valid fixture
- Malformed/unsupported input fixture
- Timeout/cancellation or HTTP failure where applicable
- Secret redaction
- Regression against NP-001..NP-120 contracts
- Windows validation for UI/OS-specific behavior where applicable

## Completion Acceptance
```powershell
cargo fmt --all -- --check
cargo check --workspace
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

## Codex Prompt
Read README.md, AGENTS.md, docs/ARCHITECTURE.md, docs/IPC_PROTOCOL.md and the existing
V4 task set. Implement only NP-127: 手动、启动、定时自动更新策略. This is an additive S11 feature; do not alter
or renumber NP-001..NP-120. Modify only the relevant allowed paths. Reuse existing V4
domain models. Add deterministic tests and explicit error handling. Network operations
must have bounded timeouts and cancellation. Subscription update failure must preserve
the last known-good version. Never log credentials, access tokens, private keys or full
proxy secrets. Run the acceptance commands and report changed files, tests and risks.
