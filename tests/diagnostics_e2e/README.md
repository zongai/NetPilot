# diagnostics_e2e (V5)

Harness notes. Run unit/integration tests via:

```
cargo test --workspace
```

Windows E2E requires starting `netpilot-core.exe` and Desktop against Named Pipe `\\.\pipe\netpilot-core`.
