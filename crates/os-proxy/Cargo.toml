[package]
name = "netpilot-os-proxy"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "Windows system proxy (Internet Settings + WinHTTP)"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
