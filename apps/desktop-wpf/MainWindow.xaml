<Window x:Class="NetPilot.Desktop.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetPilot"
        Height="640"
        Width="960"
        MinHeight="400"
        MinWidth="640"
        WindowStartupLocation="CenterScreen">
  <DockPanel>
    <Menu DockPanel.Dock="Top">
      <MenuItem Header="_File">
        <MenuItem Header="E_xit" Click="Exit_Click"/>
      </MenuItem>
      <MenuItem Header="_Help">
        <MenuItem Header="_About" Click="About_Click"/>
      </MenuItem>
    </Menu>
    <StatusBar DockPanel.Dock="Bottom">
      <StatusBarItem>
        <TextBlock x:Name="StatusText" Text="Starting…"/>
      </StatusBarItem>
    </StatusBar>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="200"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <ListBox x:Name="NavList"
               Grid.Column="0"
               Margin="8"
               SelectionChanged="NavList_SelectionChanged">
        <ListBoxItem Content="Home" Tag="home"/>
        <ListBoxItem Content="Proxies" Tag="proxies"/>
        <ListBoxItem Content="Rules" Tag="rules"/>
        <ListBoxItem Content="Connections" Tag="connections"/>
        <ListBoxItem Content="Logs" Tag="logs"/>
        <ListBoxItem Content="Diagnostics" Tag="diagnostics"/>
        <ListBoxItem Content="Subscriptions" Tag="subscriptions"/>
        <ListBoxItem Content="Settings" Tag="settings"/>
      </ListBox>
      <Border Grid.Column="1" Margin="16" Padding="12" Background="#FFF8F8F8">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <TextBlock x:Name="BodyText"
                     TextWrapping="Wrap"
                     FontSize="16"
                     FontFamily="Segoe UI"/>
        </ScrollViewer>
      </Border>
    </Grid>
  </DockPanel>
</Window>
